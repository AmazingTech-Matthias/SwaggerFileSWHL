{
  "swagger": "2.0",
  "info": {
    "version": "1.0.0",
    "title": "SharePoint Site API"
  },
  "host": "testtraveflow.traveport-test.de",
  "basePath": "",
  "schemes": ["https"],
  "consumes": [],
  "produces": ["application/json"],
  "paths": {
    "/{ProjectSite}/_api/web?$select=Url,ServerRelativeUrl,Title": {
      "get": {
        "operationId": "GetSiteInformation",
        "parameters": [
          {
            "name": "ProjectSite",
            "in": "path",
            "required": true,
            "type": "string"
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "schema": {
              "$ref": "#/definitions/SiteInformation"
            }
          }
        }
      }
    }
  },
  "definitions": {
    "SiteInformation": {
      "type": "object",
      "properties": {
        "Title": {
          "type": "string"
        },
        "Url": {
      "type": "string"
            },
        "ServerRelativeUrl": {
        "type": "string"
         }
      }
    }
  }
}
